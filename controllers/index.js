const participant = require('./participant');
const questionnaire = require('./questionnaire');
const userResponse = require('./participant-response');

module.exports = {
  participant,
  questionnaire,
  userResponse
};
